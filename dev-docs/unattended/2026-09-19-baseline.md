# noj-cli 纯 TS 重写基线快照（2026-09-19）

> 本文件由 Task 1 生成，冻结重写前的绿色基线。后续所有 Task 的成功判据均与此对照。

## 冻结提交

| 项 | 值 |
|---|---|
| 冻结 SHA（重写起点，jj change） | `c5b0394fe3ed5d0db0e0c15148c17938eb3aeaa5` |
| 冻结 SHA 的父提交（计划提交） | `3fab63a57e449ba8e8c4d02dfaac43869f910d07` — `docs(root): noj-cli 纯 TS 重写实施计划（26 个 Task，subagent per task 执行）` |
| 记录日期 | 2026-09-18（UTC；计划文件名日期 2026-09-19） |
| Deno 版本 | `deno 2.9.6 (stable, release, x86_64-unknown-linux-gnu)` |
| V8 / TypeScript | `v8 15.0.245.2-rusty` / `typescript 6.0.3` |
| 模块目录 | `noj-cli/` |
| 模块配置 | `noj-cli/deno.json`（name `@noj/cli`，version `0.9.5`） |

> **说明（重要）**：`c5b0394f…` 是 Step 1 执行 `jj log --no-graph -r '@'` 时捕获的空 working-copy change（`jj status` 为 "The working copy has no changes"，无 diff），其父为计划提交 `3fab63a5…`。二者树内容相同，即重写前的确切代码状态。
>
> 完成 Task 1 的 `jj describe` 后，该空 change 已被重写（rewrite）为 Task 1 提交：`chore(cli): 接入 Cliffy 依赖并记录重写基线`，稳定标识 **change_id `zxkmnzmq`**（`jj log -r zxkmnzmq` 可定位；commit_id 会因本次文档改写而再次变化，故不在此固化），`c5b0394f…` 成为 abandoned 提交。**后续 Task 若要机器化比对，请以永久历史中的父提交 `3fab63a57e449ba8e8c4d02dfaac43869f910d07` 作为稳定锚点**（`c5b0394f…` 在 `jj gc` 后可能不可达）。

## 基线测试（`cd noj-cli && deno task test`）

命令与输出（取尾 3 行）：

```bash
$ cd noj-cli && deno task test 2>&1 | tail -3
fileExists: 存在的文件返回 true ... ok (2ms)
fileExists: 不存在/目录返回 false ... ok (224µs)

ok | 323 passed | 0 failed (2s)
```

**基线：`ok | 323 passed | 0 failed (2s)`（exit code 0）**

## 基线门禁（`cd noj-cli && deno task check`）

命令与输出（取尾 3 行）：

```bash
$ cd noj-cli && deno task check 2>&1 | tail -3
Task check deno fmt --check && deno lint && deno check src/mod.ts src/cli.ts
Checked 108 files
Checked 106 files
```

`deno task check` = `deno fmt --check && deno lint && deno check src/mod.ts src/cli.ts`。
**基线：exit code 0（fmt 108 文件 / lint 106 文件 / type-check 通过）**

## Task 1 之后（对照）

| 指标 | 基线 | Task 1 后 |
|---|---|---|
| `deno task test` | 323 passed / 0 failed | **324 passed / 0 failed** |
| `deno task check` | exit 0（fmt 108 / lint 106） | exit 0（fmt 109 / lint 107） |
| `@cliffy/command` | 未接入 | `jsr:@cliffy/command@1.2.1`（`deno add` 写入 `noj-cli/deno.json` 与 `deno.lock`） |
