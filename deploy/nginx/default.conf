# Neuro OJ 公测反向代理（Nginx，容器内只处理 HTTP）
#
# 说明：容器内的 Nginx 不处理 TLS。TLS 终止由宿主机 Nginx / Caddy /
# 云负载均衡等外部边缘完成，再将 HTTP 流量转发到本容器的 80 端口。
#
# 若外部边缘已设置 X-Forwarded-Proto=https，noj-ui/noj-core 会据此生成
# Secure Cookie 与正确跳转。
#
# 所有业务流量转发到 noj-ui（ui:3000）；/healthz 单独转发到 noj-core。

server {
    listen 80;
    server_name _;

    # 上传支持包等允许较大体积
    client_max_body_size 64m;

    # 基础安全响应头（HSTS 等由外部 TLS 终止层负责）
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' data:; connect-src 'self' https:; object-src 'none'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'" always;

    # 运维探活：直接打到 noj-core /health
    location = /healthz {
        proxy_pass http://core:8000/health;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 其余流量全部交给 noj-ui（Nitro 内部代理 /api 到 noj-core）
    location / {
        proxy_pass http://ui:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # SSE / 长连接
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 1h;
    }
}
