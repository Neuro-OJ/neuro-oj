# --- OS ---
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# --- IDE ---
.idea/
.vscode/
*.code-workspace
.trea/

# --- Environment ---
.env
.env.*
!.env.example
!.env.e2e.template
!.env.prod.example

# --- Deno ---
.deno_cache/
deno.lock  # 不锁定依赖版本——社区库遵循 semver 即可

# --- Node.js / npm ---
node_modules/
.nuxt/
.output/
dist/
.cache/
*.tsbuildinfo

# --- Logs ---
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Rust ---
target/
*.rs.bk
**/*.rs.bk

# --- Python (evaluate.py scripts) ---
__pycache__/
*.pyc
*.pyo
*.egg-info/

# --- Docker ---
docker-data/

# --- 生产备份 ---
# PostgreSQL dump 包含用户数据，不得进入 Git。
/backups/

# --- Staging 验收产物 ---
/artifacts/staging/

# --- Jujutsu (jj) ---
.jj/repo/op_heads/
.jj/repo/store/

# --- AI tool migration artifacts ---
# `source-command-opsx-*` 是 opsx 命令的 source-command 包装 wrapper，
# 与 `.agents/skills/openspec-*` 功能重复，迁移完成后应删除；
# 在清理完成前临时忽略，防止误提交。
.agents/skills/source-command-opsx-*/

# --- Build Artifacts ---
tmp/
coverage/
*.wasm

# --- Problem Data (noj-core) ---
# Support packages are build artifacts, not versioned
noj-core/data/packages/
!noj-core/data/packages/.gitkeep
# Local storage backend (problem-bundle-import: separated from build artifacts)
noj-core/data/storage/

# --- Dev tool locks (devtool.sh advisory locks for concurrent safety) ---
scripts/dev/locks/
# PID 文件由 `devtool.sh start` 启动模块时写入，用于模块存活追踪
scripts/dev/logs/*.pid

# --- IDE metadata (project-local, not shareable) ---
*.iml
*.idea/
.playwright-mcp/
aa.txt

# --- Rust profiling ---
*.profraw
*.profdata

# --- Docker buildx cache ---
.buildx/

.reasonix/docs/review-snapshots/
docs/review-snapshots/
.reasonix/

# --- LMCC raw problem archive (downloaded from official site; not committed) ---
/lmcc-problems/
