# Neuro OJ vs Hydro OJ：差异分析与未来发展方向

> 文档日期：2026-06-27
> 对比基于 Neuro OJ (NOJ) 当前 Phase 1 实现与 Hydro OJ v4.58.3 完整功能集。
> **核心前提：NOJ 面向 LMCC（大语言模型能力认证），Hydro OJ 面向传统 OI（信息学竞赛）。**
> 这导致两者在架构理念上存在根本性差异，而非简单的功能多寡。

---

## 目录

1. [LMCC 与 OI：根本范式差异](#1-lmcc-与-oi根本范式差异)
2. [现状功能对比总表](#2-现状功能对比总表)
3. [架构差异](#3-架构差异)
4. [由于范式差异导致的取舍](#4-由于范式差异导致的取舍)
5. [我们的优势](#5-我们的优势)
6. [通用功能缺失（需要追赶）](#6-通用功能缺失需要追赶)
7. [未来发展方向建议](#7-未来发展方向建议)
8. [总结：核心差异化路线](#8-总结核心差异化路线)

---

## 1. LMCC 与 OI：根本范式差异

这是理解 NOJ 与 Hydro OJ 所有差异的起点。两者面对的评价对象和场景完全不同。

| 维度 | 传统 OI（Hydro OJ） | LMCC / AI 评测（NOJ） |
|------|--------------------|----------------------|
| **评价对象** | 人类竞争选手的算法能力 | LLM 的编程与工程综合能力 |
| **问题本质** | 有标准答案的封闭问题 | 可半开放的工程问题 |
| **答案形式** | 输出文件 / stdout | 完整源代码文件 |
| **评价维度** | 正确性 + 效率（时间/内存） | 正确性 + 代码质量 + 工程规范 |
| **测试用例** | 统一的输入→期望输出对 | 由自定义评测脚本决定 |
| **评分逻辑** | 通用 checkers（diff/testlib/SPJ） | 问题独有 evaluate.py（任意逻辑） |
| **评测环境** | 统一编译+运行环境 | 每道题独立 Docker 镜像+命令 |
| **赛制** | ACM/OI/IOI 标准化赛制 | 考试/认证模式 |
| **语言** | C++ 主导 | **LMCC 仅支持 Python** |
| **防作弊** | 代码相似度检测 | 需检测 AI 辅助程度 / LLM 指纹 |
| **核心指标** | AC 时间、排名 | 综合得分、工程能力评估 |

### 1.1 这对架构意味着什么

**Hydro OJ 的架构假设**：所有题目共享同一套评测基础设施（统一的编译、运行、对比系统），题的差异仅在于输入数据和期望输出。

**NOJ 的架构假设**：每道题是独立的评测微世界（自定义 Docker 镜像 + 自定义评测脚本）。题与题之间评测逻辑可能完全不同。

这就是 NOJ 选择 **support package 模式**（每道题打包自己的评测脚本）而非 Hydro OJ 的 test case + checker 模式的原因。这是范式差异导致的不同抽象层次。

### 1.2 一个具体例子

- **Hydro OJ 的一道 OI 题**：10 个测试用例，每个输入→期望输出，用 diff 对比。
- **NOJ 的一道 LMCC 题**：LLM 编写完整的 REST API。评测脚本启动容器、发 HTTP 请求、验证响应、检查代码风格、运行单元测试，综合评分。

### 1.3 LMCC 语言定位

LMCC 官方仅支持 Python，这是 CCF 的明确定位。因此 NOJ **不做**多语言支持——这不是取舍，而是正确定位。我们的评测栈只需要深度优化 Python 单语言场景。

---

## 2. 现状功能对比总表

| 功能领域 | 子功能 | Hydro OJ | Neuro OJ | 备注 |
|---------|--------|----------|----------|------|
| **用户系统** | 注册/登录 | 完整 | 完整 | NOJ 额外有速率限制和失败退避 |
| | OAuth 登录 | GitHub/Google/OIDC | 无 | 应追赶 |
| | WebAuthn/Passkey | 支持 | 无 | 锦上添花 |
| | 2FA | 支持 | 无 | 锦上添花 |
| | 用户主页 | 含统计/图表 | 基础 | NOJ 较简单 |
| | 个人简介(bio) | 支持 | 支持 | |
| | 站内信/通知 | 用户间+系统通知 | 无 | 应追赶 |
| **题目管理** | CRUD | 完整 | 完整 | |
| | 分类/标签 | 树形多级分类 | 树形多级分类 | 对等 |
| | 难度标记 | 多级 | 基础（easy/medium/hard） | |
| | 题目导入导出 | FPS/HustOJ/QDUOJ 导入 | 无 | 应追赶 |
| | 多语言题目内容 | 语言覆写文件 | 无 | 国际化友好 |
| | 随机选题 | 支持 | 无 | |
| | 题目题解 | 用户提交+投票 | 无 | 应追赶 |
| **评测系统** | 评测执行 | Go 沙箱 daemon | Rust + Docker 沙箱 | 技术路线不同 |
| | 判题模式 | 8 种模式 | 仅标准对比 | 应扩展 |
| | 子任务计分 | min/sum 规则 | 无（由 evaluate.py 自定） | 范式差异 |
| | 检查器 | 5 种内置 | 无（由 evaluate.py 自定） | 范式差异 |
| | 语言支持 | C/C++/Python/Java/... | **仅 Python（LMCC 定位）** | 有意识不做 |
| | 容器预热 | 有限 | 容器池+自动扩缩容 | NOJ 更精细 |
| | 资源限制 | 支持 | cgroup 内存峰值 | 对等 |
| | 评测队列 | MongoDB 轮询 | Redis MQ（LPUSH/BRPOP） | NOJ 支持水平扩展 |
| | 编译缓存 | 支持 | 无 | LMCC 不需要 |
| **竞赛** | ACM/ICPC 赛制 | 支持 | 延后做 | LMCC 赛事可能引入 |
| | OI/IOI 赛制 | 支持 | 延后做 | LMCC 赛事可能引入 |
| | 考试/认证模式 | 作业模式 | 无 | LMCC 需要 |
| | 实时榜单 | WebSocket 推送 | 无 | 非 LMCC 需求 |
| | 封榜/滚榜 | 支持 | 无 | 非 LMCC 需求 |
| | 赛时提问 | Clarification | 无 | |
| | 气球追踪/打印服务 | 现场赛用 | 无 | 非必需 |
| **讨论/社区** | 题目讨论 | 完整 | 无 | 应追赶 |
| | 训练计划 | DAG 路径+前置依赖 | 无 | LMCC 备考路线图可对标 |
| | 题解系统 | 投票+回复 | 无 | |
| **管理后台** | 用户管理 | 完整 | 基础 | NOJ 有角色管理 |
| | 题目管理 | 完整 | 完整 | |
| | 提交管理 | 完整（含重测） | 基础 | |
| | 分类管理 | 支持 | 支持 | |
| | 系统设置 | 键值设置面板 | 无 | 应追赶 |
| | 操作审计日志 | 支持 | 无 | |
| | 黑名单 | 用户/IP | 无 | 安全基础 |
| **基础设施** | 插件系统 | Cordis 插件架构 | 无 | NOJ 暂不需要 |
| | 多租户/域 | 完整 domain 隔离 | 延后做 | 未来轻量方案 |
| | 文件存储 | 本地/S3 | 无 | 应追赶 |
| | 搜索 | Elasticsearch/Sonic | 无 | 应追赶 |
| | WebSocket | 完整 pub/sub | 无 | 应追赶 |
| | Prometheus 指标 | 支持 | noj-judge 有 | core 缺 metrics |
| | 健康检查 | 支持 | /health + degraded 模式 | NOJ 更细致 |
| | Structured log | 可配置 | 生产安全日志 | |
| **前端** | 渲染方式 | Nunjucks SSR + React | Nuxt 4 + Vue 3 SPA | 技术路线不同 |
| | 代码编辑器 | Monaco | Monaco CDN | |
| | Markdown 渲染 | markdown-it+KaTeX | markdown-it+KaTeX+DOMPurify | |
| | 主题切换 | 支持 | 无 | |
| | 实时更新 | WebSocket | 轮询（1.5s/2s） | 应追赶 |
| **测试** | 单元测试 | 有限 | 较完整 | |
| | E2E 测试 | 有限 | 完整（52 测试） | |
| | Docker 沙箱测试 | 有限 | 完整（6 套件） | |

---

## 3. 架构差异

### 3.1 技术栈对比

| 维度 | Hydro OJ | Neuro OJ |
|------|----------|----------|
| **运行时** | Node.js (Koa 3 + Cordis) | Deno (Hono) |
| **数据库** | MongoDB（文档型） | PostgreSQL（关系型）+ Drizzle ORM |
| **消息队列** | MongoDB 轮询 | Redis MQ（独立消息层） |
| **评测沙箱** | Go 独立 daemon | Rust + Docker API（bollard） |
| **前端** | React 19 + Nunjucks + Webpack | Vue 3 + Nuxt 4 + Vite |
| **插件系统** | Cordis 插件框架 | 无插件系统 |
| **包管理** | npm / yarn | deno.json / npm 兼容 |
| **部署** | Nix flake + 一键安装脚本 | Docker Compose |
| **CSS** | Mantine UI + SCSS | Tailwind CSS |

### 3.2 关键架构决策差异

| 决策 | Hydro OJ | Neuro OJ | 分析 |
|------|----------|----------|------|
| **存储** | MongoDB（灵活 schema） | PostgreSQL（严格 schema） | Hydro 的通用 document 表需要 NoSQL。NOJ 的关系型设计明确了数据关系 |
| **评测隔离** | 共享编译+独立运行 | 每道题独立 Docker 镜像 | NOJ 更重但更灵活，适合 LMCC |
| **状态传递** | MongoDB 轮询 | Redis MQ（LPUSH/BRPOP） | NOJ 的 MQ 支持水平扩展 |
| **请求处理** | Koa 中间件 | Hono 路由 | 两者都是现代框架 |
| **API 风格** | 类 GraphQL（Query/Mutation/Subscription） | 传统 RESTful | REST 更普遍 |

### 3.3 项目成熟度

| 指标 | Hydro OJ | Neuro OJ |
|------|----------|----------|
| 项目起始 | 2018 年（7 年+） | **2026 年 6 月（不到 1 个月）** |
| 包/模块数 | 20+ 官方包 + 第三方插件 | 3 个子模块 |
| 数据库表/集合 | 15+ 集合 | 6 张表 |
| 开发者 | 社区团队 | 个人/小团队 |

Hydro OJ 是经过 7 年迭代的成熟项目。NOJ 极其年轻，功能差距是自然的时间积累，关键是差异化发展。

---

## 4. 由于范式差异导致的取舍

以下区分两类：**不做**（范式不匹配，永远不做）和 **延后做**（需要但非 Phase 1 核心）。

### 4.1 我们不做：多语言支持

**Hydro OJ**：支持 C/C++/Python/Java/JavaScript/Go 等多种语言，每种有独立编译链。

**NOJ 的决定**：**仅支持 Python。**

**原因**：LMCC 官方明确定位于 Python。投入精力做多语言编译链对 NOJ 没有价值。相反，我们应该深度优化 Python 生态：pip 依赖管理、Python 版本选择、常用库预装。

### 4.2 我们不做：通用 Checker / Subtask 体系

**Hydro OJ**：5 种内置 checker、subtask 计分、test case 管理界面。

**NOJ 的决定**：全部交给 evaluate.py。

**原因**：LMCC 评测脚本需要任意复杂逻辑（HTTP 验证、代码审查、API 调用），无法被通用 checker 覆盖。让出题者直接用 Python 写评测逻辑换来无限灵活性。

### 4.3 延后做：竞赛赛制引擎

**Hydro OJ**：6 种竞赛规则引擎（ACM/ICPC、OI、IOI、Strict IOI、Ledo、Homework）。

**NOJ 的现状**：当前聚焦考试/认证模式（指定题集+时长+自动评分）。

**规划**：竞赛赛制（尤其是 OI 式 subtask 计分、ACM 式罚时排名）在 LMCC 场景中同样有应用价值——例如举办 LMCC 热身赛或能力挑战赛。但 Phase 1 不需要这些复杂规则引擎，优先做基础考试模式。**Phase 2 可根据需求引入轻量竞赛规则**。封榜、滚榜、气球追踪等现场赛功能大概率不做。

### 4.4 延后做：多租户 Domain 系统

**Hydro OJ**：完整的多租户 domain 隔离，每个 domain 独立用户/角色/权限/题目。

**NOJ 的现状**：当前单体用户空间，为简洁优先。

**规划**：多租户是未来可能的方向（支持多个机构独立使用同一部署），但 Phase 1 不需要。未来的实现会采用轻量方案（如 org_id 作用域隔离），不需要 Hydro OJ 那么重的 domain 层。

### 4.5 我们不做：Hack 系统

**Hydro OJ**：提交后可 hack 他人代码。

**NOJ 的决定**：完全不考虑。

**原因**：传统 OI 社区文化，与 LMCC 认证无关。

### 4.6 延后做：社区/讨论区/题解系统

**Hydro OJ**：完整的讨论、题解（含投票）、站内信。

**NOJ 的规划**：社区和讨论区是未来方向，但需要成熟的用户基础后再启动。现阶段优先交付核心评测+考试体验。**题解系统**可在 Phase 2 先行（作为备考学习资源），论坛等社交功能放更后。

---

## 5. 我们的优势

### 5.1 现代技术栈

| 方面 | Neuro OJ | Hydro OJ | 评价 |
|------|----------|----------|------|
| 运行时 | Deno（原生 TS） | Node.js（需 tsc） | Deno 零配置 |
| 前端 | Nuxt 4 + Vue 3 | Webpack + React + Nunjucks | Nuxt 体验更现代 |
| 数据库 | PostgreSQL + Drizzle（类型安全） | MongoDB（无 schema 约束） | 更可靠 |
| CSS | Tailwind CSS | Mantine UI + SCSS | 更轻量 |
| 评测沙箱 | Rust（内存安全） | Go sandbox daemon | Rust 资源控制更精细 |
| 测试 | 52 E2E + 单元测试 | 有限 | 测试更体系化 |

### 5.2 容器池和评测架构优势

NOJ 的 noj-judge 做得比 Hydro OJ 细致：

- **PoolManager**：预热容器池，RAII Guard 自动归还
- **Auto-scaler**：滑动窗口 QPS + 排队时间感知 + 自动扩缩容
- **Prometheus /metrics**：指标可观测
- **健康检查**：定期检测容器健康

Hydro OJ 每次启动新容器，没有池化管理和自动扩缩容。

### 5.3 安全设计

- 生产模式下自动截断 submission_id、隐藏分值、脱敏数据库密码
- 登录速率限制：IP 窗口 + 账号窗口 + 失败退避 + 账号锁定
- bcrypt cost 12

### 5.4 天然水平扩展

Redis MQ（LPUSH/BRPOP）生产者-消费者模式原生支持多个 noj-judge 实例并行。

### 5.5 测试体系

Phase 1 就建立了完整测试金字塔：单元测试、跨模块 E2E（52 测试）、Docker 沙箱 E2E。Hydro OJ 作为成熟项目测试覆盖反而较弱。

---

## 6. 通用功能缺失（需要追赶）

以下是不涉及范式差异、纯粹是通用 OJ 平台的功能缺失。

### P0 - 核心缺失

| 功能 | 说明 | 工作量 |
|------|------|--------|
| **WebSocket 实时推送** | 当前轮询替代，对用户体验不佳 | 中 |
| **文件存储系统** | 支持包路径现为本地路径，生产需 S3/minio | 中 |

### P1 - 重要功能

| 功能 | 说明 | 工作量 |
|------|------|--------|
| **提交重测** | 管理员手动触发重测 | 小 |
| **题目搜索** | 搜索标题/描述 | 中 |
| **管理后台系统设置** | 在线修改系统参数 | 中 |
| **权限系统细化** | 目前仅两角色，缺少细分 | 中 |
| **操作审计日志** | 记录敏感操作 | 小 |
| **黑名单** | IP/用户封禁 | 小 |

### P2 - 锦上添花

| 功能 | 说明 | 工作量 |
|------|------|--------|
| OAuth 登录 | GitHub/Google 一键登录 | 中 |
| 题目导入导出 | FPS 格式导入 | 中 |
| 主题切换 | 亮/暗色模式 | 小 |
| 个人设置页 | 修改密码、邮箱、偏好 | 中 |
| 数据可视化 | 用户统计图表 | 中 |
| Webhook 集成 | 评测完成通知外部系统 | 小 |
| 计划任务 | 定时数据清理/统计 | 小 |

---

## 7. 未来发展方向建议

### 7.1 近期：补齐通用缺失 + 深化 LMCC 差异化

**通用补齐：**
- WebSocket 替代轮询
- 提交重测
- 搜索功能
- 权限细化
- 审计日志 + 黑名单
- 文件存储（S3/minio）

**LMCC 差异化深耕：**
- **LLM 特有评测维度**：代码质量、注释规范、错误处理、安全性
- **混合评测模式**：一道题含多个评测阶段（编译→测试→代码审查→报告）
- **交互式评测**：多轮对话式评测（LLM 根据反馈修改代码）
- **提示词注入防护**：防止 LLM 生成的代码攻击评测环境
- **Python 生态深度优化**：pip 依赖缓存、常用库预装、版本选择

### 7.2 中期：考试/认证系统

- **考试模式**：创建考试→指定题集→设定时长→自动评分
- **成绩报告**：多维度能力评估报告（不单是分数）
- **防作弊**：IP 记录、浏览器指纹、代码风格分析
- **证书生成**：通过后自动生成 PDF 证书
- **题目分组**：按能力维度分组
- **自适应难度**：根据表现动态调整后续题目

### 7.3 长期：差异化蓝海

以下方向是 Hydro OJ 无法（或不适合）做的：

**1. AI 辅助评测**
- 使用 AI 对代码质量自动评分
- LLM 输出指纹识别——区分"理解后写出"与"直接抄写"
- 多角度评估报告：可读性、效率、安全性、健壮性

**2. 面向 LLM 的题目生态**
- 工程型题目：补全代码 / 代码审查 / Bug 定位 / 重构 / API 设计
- 传统 OJ 题是"给定输入，计算输出"；LMCC 题是"给定需求，实现功能"

**3. 非同步考试模式**
- LLM 认证不需要同时参加
- 随到随考（大规模题库+随机抽题）
- AI 监考：浏览器中检测可疑行为

**4. 开放的评测协议**
- 标准化 NOJ 的评测协议
- 任何人都可以编写评测脚本作为题目
- 建立 LMCC 题目市场

**5. IDE 工具链集成**
- 与 Cursor、VS Code 等 IDE 集成
- LLM 在 IDE 中完成题目后一键提交
- API 优先设计，方便 CI/CD 集成

### 7.4 不应做的方向

| 方向 | 原因 |
|------|------|
| 插件系统 | 小团队维护成本高 |
| 多语言支持 | LMCC 官方仅 Python |

---

## 8. 总结：核心差异化路线

```
                    Hydro OJ（传统 OI，7 年）
                   /                        \
        通用 OJ 功能（对比表 2 的应追赶项）
                   \                        /
                    Neuro OJ（LMCC 认证，1 个月）
                           |
          ┌────────────────┼────────────────┐
          ▼                ▼                ▼
    通用补齐          LMCC 差异化        蓝海方向
    ──────────    ──────────────    ────────────────
    WebSocket     LLM 代码指纹      AI 辅助评分
    搜索          Python 生态深耕    工程型题目
    重测          混合评测模式       非同步考试
    审计日志      多维度评估报告     IDE 集成
    黑名单        考试认证系统       开放评测协议
    文件存储      自适应难度         LMCC 题目市场
```

不要试图在通用 OJ 功能上与 Hydro OJ 正面竞争。通用功能补齐到"够用"即可。

**深耕 LMCC 这个 Hydro OJ 无法覆盖的新市场——这才是长期护城河。**

### 一句话总结

- **明确不做**（范式不匹配）：多语言、通用 checker/subtask 体系、Hack 系统
- **延后做**（排期靠后）：竞赛赛制（LMCC 赛事可用）、多租户（轻量方案）、社区/题解/讨论区（用户基础成熟后）
- **加速做**（通用短板）：WebSocket、搜索、重测、审计日志、黑名单、文件存储
- **蓝海做**（差异化）：AI 辅助评分、混合评测、非同步考试、IDE 集成、开放评测协议

