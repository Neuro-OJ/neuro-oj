## 1. 管理端 gateway 错误处理

- [x] 1.1 统一管理端 LLM gateway 路由的 HTTP 与网络错误映射，覆盖 Provider、用量和配额读写入口；通过路由测试验证 400/404、上游不可用和未知异常的响应。
- [x] 1.2 运行 core gateway 路由相关测试与类型检查，确认成功响应结构保持不变。

## 2. 评测与 E2E 断言

- [x] 2.1 保持 P1001 运行期异常直接终止评测并产生 judge `error`，收紧 pipeline E2E 断言拒绝 `finished + 0`。
- [x] 2.2 修复竞赛排名 E2E 的空数组断言，并运行相关 E2E 文件的类型检查。

## 3. 记录与验证

- [x] 3.1 新增格式符合要求的 Agent Note，记录问题、决定、备选方案和后果。
- [x] 3.2 运行 OpenSpec 校验及受影响模块的格式、lint、测试命令，并核对工作树只包含本次修复。
