## 1. 本地 E2E 启动流程

- [x] 1.1 在本地 `setup.sh` 启动 Compose 前调用既有 SDK 镜像构建脚本，并验证两个镜像均使用当前工作树构建
- [x] 1.2 确保 SDK 镜像构建失败时启动脚本返回非零状态，并验证不会继续报告 E2E 环境就绪

## 2. 回归验证与文档

- [x] 2.1 增加脚本静态回归检查，验证本地路径包含 SDK 镜像构建入口且 CI 路径不重复构建
- [x] 2.2 使用旧 SDK 镜像执行本地 LLM Gateway E2E，验证镜像自动刷新后测试通过
- [x] 2.3 运行 OpenSpec 严格校验、Shell 语法检查及相关 E2E 测试并记录结果（LLM Gateway 7/7 通过；完整一键入口的后续基础镜像拉取曾受 Docker Hub 网络 EOF 影响）
