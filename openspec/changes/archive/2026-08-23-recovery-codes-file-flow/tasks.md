## 1. 恢复码文件格式工具

- [x] 1.1 在 `noj-ui/utils/recoveryCodes.ts` 定义恢复码格式、文件大小限制、文件格式化和本地解析函数，并通过类型检查。
- [x] 1.2 为恢复码格式化、注释行、大小写/空白、重复码、非法行、空文件和数量限制补充 `noj-ui/tests/recoveryCodes_test.ts` 单元测试，确保 `deno test tests/recoveryCodes_test.ts` 通过。

## 2. 设置页保存恢复码

- [x] 2.1 在 `noj-ui/pages/settings.vue` 增加恢复码文本文件下载按钮，使用当前响应中的明文恢复码生成文件，并通过构建检查下载代码可编译。
- [x] 2.2 增加复制全部恢复码操作，并更新提示文案和保存说明，手动检查下载/复制按钮只处理当前内存中的恢复码。
- [x] 2.3 成功确认/重新生成后清空 TFA 输入值；用户状态变化、禁用 TFA 时清理所有临时 TFA 状态，并通过页面逻辑检查确认不会残留旧状态。

## 3. 登录页本地导入

- [x] 3.1 在 `noj-ui/pages/login.vue` 的 TFA 验证阶段增加文本文件选择入口和本地解析错误提示，并通过类型检查。
- [x] 3.2 展示解析出的恢复码列表，支持选择一个恢复码填入现有输入框，并保留手动输入路径；通过构建或页面静态检查确认选择后输入框同步。
- [x] 3.3 确保登录请求只发送单个 `code`，不发送文件或完整恢复码列表，不自动重试其他恢复码；通过代码检查确认调用参数只有单个 code。

## 4. 验证与收尾

- [x] 4.1 在 `noj-ui` 运行 `deno task check`（包含 `deno fmt --check`、`deno lint` 和类型检查），确认 159 个文件 lint、45 个文件类型检查均通过。
- [x] 4.2 在 `noj-ui` 运行恢复码工具单元测试及现有 UI 测试，确认 31 个测试全部通过，并完成 Nuxt 构建检查。
- [x] 4.3 运行与 TFA 相关的 core/E2E 测试：`noj-core` 定向测试 23 个通过，隔离环境 `noj-tests/e2e/31_tfa.test.ts` 7 个通过，确认一次性消费和旧码失效行为不回归。完整 E2E 编排另受工作树缺失题目 `1001` 支持包阻塞，未影响本项 TFA 测试。
- [x] 4.4 使用 `openspec validate recovery-codes-file-flow --strict` 校验变更文档通过，并核对 Issue #278（OPEN）及其下载、本地解析、单码提交验收标准。
