## 1. Redis 配置向导

- [x] 1.1 增加已有 Redis、创建本机 Redis、稍后配置三种交互选项，并验证提示说明、默认选项和非交互模式行为
- [x] 1.2 实现已有 Redis URL 的隐藏输入、格式校验和脱敏连通性检查，并验证错误输出不包含密码

## 2. 本机 Redis 创建

- [x] 2.1 实现随机密码、仅属主可读 Redis 配置文件、命名持久化卷和管理标签，并验证同名容器不会被覆盖
- [x] 2.2 实现非冲突端口检查和本机 Redis 容器启动，配置 Judge 通过 host-gateway 访问，并验证端口冲突在启动 Judge 前失败
- [x] 2.3 输出供 noj-core 和 Judge 使用的连接信息，明确要求两者使用同一 Redis、数据库和队列，并验证敏感信息不进入日志

## 3. 回归与文档

- [x] 3.1 增加本机 Redis 创建、已有容器冲突、端口冲突、延后配置和密码脱敏的离线测试，并运行全部部署脚本测试
- [x] 3.2 更新 Judge 运维文档和脚本帮助，说明 Redis 选择、连接地址差异、持久化和清理方式，并通过 OpenSpec 严格校验
