## 1. 远程入口与版本

- [x] 1.1 新增根目录 `setup.sh`，转交到生产 bootstrap 并保持参数透传；用 Shell
      smoke test 验证入口可执行且参数完整传递
- [x] 1.2 实现最新 Release 解析、显式 `--ref` 覆盖和失败提示；用离线 fake
      下载器测试自动版本、显式版本和 API 失败分支
- [x] 1.3 保持现有安装目录、配置、非交互和下载模式兼容；运行现有
      `test-install.sh` 并确认全部通过

## 2. 文档与测试

- [x] 2.1
      更新脚本帮助、脚本索引和生产部署文档，提供一条命令和先下载后检查两种方式；检查文档命令与脚本帮助一致
- [x] 2.2
      增加远程入口、版本解析、解析失败和参数透传测试；运行新增测试并确认失败分支返回非零状态
- [x] 2.3 运行 Shell、Deno、Compose 和 OpenSpec 校验；所有相关命令返回成功
