## 1. 2FA 输入模式

- [x] 1.1 更新 `noj-ui/pages/login.vue` 的 2FA 标题和默认动态验证码输入提示；通过项目级类型检查确认模板合法。
- [x] 1.2 增加动态验证码/恢复码模式切换，默认隐藏恢复码区域，点击“使用恢复码登录”后展示恢复码输入、文件导入和切回入口；通过代码检查确认模式状态和按钮类型正确。
- [x] 1.3 保持恢复码文件本地解析、恢复码选择和单个 `code` 提交逻辑，并在切换模式时清理恢复码临时状态；通过恢复码测试和代码检查确认无上传/持久化行为。

## 2. 验证与收尾

- [x] 2.1 在 `noj-ui` 运行 `deno task check`、`deno task test` 和 `deno task build`，确认前端检查、测试和构建通过；检查、31 个测试和构建均通过。
- [x] 2.2 运行 `openspec validate refine-tfa-login-input-flow --strict` 和 `git diff --check`，确认变更规范和格式检查通过；两项检查均通过。
