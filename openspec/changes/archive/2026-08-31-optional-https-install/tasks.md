## 1. 配置向导

- [x] 1.1 合并网站地址与完整网址输入，并验证 HTTPS 选择自动生成两个地址
- [x] 1.2 增加 HTTPS/临时 HTTP 选择，默认 HTTPS，并验证 HTTP 选择写入显式开关和安全提示
- [x] 1.3 配置向导先写入临时文件，最后确认后原子替换正式配置；根据已有邮件配置显示回车和跳过的真实含义

## 2. 运行时安全策略

- [x] 2.1 修改后端生产地址校验，验证默认拒绝 HTTP、显式开关后允许 HTTP
- [x] 2.2 修改前端生产 Cookie 行为和 Compose 环境变量，验证 HTTP 临时模式可以登录

## 3. 测试与文档

- [x] 3.1 增加脚本、后端和前端配置测试，覆盖 HTTPS、HTTP 和默认安全门禁
- [x] 3.2 更新配置模板、部署文档和用户提示，说明证书不会由脚本自动安装
- [x] 3.3 运行格式检查、语法检查、回归测试和 OpenSpec 校验
- [x] 3.4 增加配置暂存、取消不落盘和邮件提示的回归测试，并更新部署文档
