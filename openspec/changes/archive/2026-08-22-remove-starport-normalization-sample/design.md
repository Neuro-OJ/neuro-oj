## 决策

删除 P1004 的完整题源，而不是隐藏题目。这样 `dev-setup`
和题目导入不会重新创建它。

端到端测试固定选取 P1001，因此将其注释和 `CODE_SAMPLES` 的接受答案从星港 JSON
协议改为 A+B
的标准输入输出。错误答案、超时、内存超限、运行时错误和语法错误夹具保持测试目的不变。

## 验证

重新构建支持包，确认只剩 A+B 对应的构建包；执行格式、lint、相关 E2E 文件的测试与
OpenSpec 校验。
