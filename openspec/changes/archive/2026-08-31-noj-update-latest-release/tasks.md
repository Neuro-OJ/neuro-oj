## 1. 最新版本解析

- [x] 1.1 为生产 CLI 增加通过 GitHub latest Release API 获取最新稳定 tag 的逻辑，并验证 HTTPS、超时、重试和 tag 格式检查可独立测试
- [x] 1.2 为 `noj update` 增加 `--latest` 参数，验证默认固定版本升级和显式最新版本升级互不改变

## 2. 安全升级流程

- [x] 2.1 实现最新版本展示、同版本无操作成功返回，以及升级前后的配置暂存/提交，验证查询失败和升级失败时 `.env.prod` 不变
- [x] 2.2 让最新版本升级复用现有部署文件同步、备份、镜像校验、Compose 升级和健康检查流程，验证数据卷不会被删除

## 3. 文档与回归验证

- [x] 3.1 更新 README、生产部署文档和 CLI 帮助，说明 `noj update --latest` 的稳定版本范围、回滚方式和 RC 固定版本用法
- [x] 3.2 扩展无 Docker CLI/部署脚本测试，运行脚本语法检查、部署测试、OpenSpec 严格校验和文档链接检查
