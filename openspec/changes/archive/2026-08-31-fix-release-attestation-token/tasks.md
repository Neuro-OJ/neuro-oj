## 1. 发布验证配置

- [x] 1.1 为正式镜像验证步骤注入 `GH_TOKEN: ${{ github.token }}`，并通过 YAML 结构检查确认配置位于验证任务环境中
- [x] 1.2 在供应链检查中要求来源证明查询令牌配置，并运行正向检查确认现有工作流通过

## 2. 回归验证

- [x] 2.1 在供应链测试中移除令牌配置并确认检查失败，同时保留其他负向测试通过
- [x] 2.2 运行 shell 语法检查、供应链测试和 OpenSpec 严格校验，确认正式发布验证具备可合并状态
