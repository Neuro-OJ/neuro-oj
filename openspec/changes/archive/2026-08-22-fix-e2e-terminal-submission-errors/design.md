## 决策

保留用户代码单次调用的 5 秒限制，并将评测器总时限同样设为 5
秒。总时限覆盖容器创建、支持包注入和评测编排，不能采用 A+B
算法本身的毫秒级预期执行时间。

提交状态机的终态为 `finished` 与 `error`。轮询遇到 `error`
时应立即抛错，并携带评测 verdict，避免将系统故障伪装成排队超时。

## 验证

重建支持包、运行格式与 lint，并运行相关 E2E 测试；完整 CI 将验证双容器并发环境。
