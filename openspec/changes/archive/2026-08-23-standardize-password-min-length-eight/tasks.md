## 1. 统一认证代码规则

- [x] 1.1 将 `noj-ui/utils/validatePassword.ts` 的最小长度和错误消息改为 8 位，并覆盖注册、修改密码和重置密码页面的复用路径；通过前端密码校验测试验证少于 8 位失败、恰好 8 位且字符组成合规时通过。
- [x] 1.2 核对并补充后端注册、修改密码和重置密码的 8 位边界测试，确保 `validatePasswordStrength()` 与路由预检保持一致；通过核心认证测试验证。

## 2. 同步提示、规范和文档

- [x] 2.1 更新注册、登录、修改密码和重置密码页面中仍不准确的密码提示，并确认所有有效用户文档统一描述至少 8 位及现有字符组成规则。
- [x] 2.2 更新根项目/核心模块开发文档、`openspec/specs/user-auth` 主规范和测试计划中残留的 12 位密码描述；通过限定路径检索确认无有效文档残留。

## 3. 验证与收尾

- [x] 3.1 运行 `noj-ui` 的格式、lint、类型检查、测试和构建，以及 `noj-core` 的格式、lint、类型检查和认证测试；确认全部通过。
- [x] 3.2 运行 `openspec validate standardize-password-min-length-eight --strict`、`git diff --check` 和密码规则检索，确认规范、格式与规则一致性检查通过。
