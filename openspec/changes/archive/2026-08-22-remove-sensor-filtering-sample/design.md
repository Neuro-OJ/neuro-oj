## 决策

将 P1002 从版本控制的题目源中完全删除，而不是仅在题库页面隐藏。这样新的
`dev-setup` 或题目导入不会再次创建该题。

现有开发机可能残留
`data/packages/1002.zip`。该文件是忽略版本控制的构建产物，也一并删除，避免本地再次导入旧支持包。

## 保留的示例题

- P1001：A+B Problem
- P1004：星港舱门报码归一化

## 测试策略

删除 P1002 专属模板断言，保留 P1001
的模板、题目不存在和未认证访问断言；随后重新构建全部题目支持包并运行相关端到端测试。
