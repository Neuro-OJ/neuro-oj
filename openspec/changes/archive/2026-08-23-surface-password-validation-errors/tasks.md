## 1. 完善共享密码校验

- [x] 1.1 扩展前端密码校验函数以接收用户名和邮箱上下文，并覆盖用户名/邮箱前缀限制；补充少于 8 位、字符组成不完整、前缀冲突和合法密码测试，运行 `deno test -A tests/validatePassword_test.ts` 验证。
- [x] 1.2 让 `PasswordField` 转发 blur 事件，并保持现有 v-model、focus 清错和可见性切换行为；通过 `deno task check` 验证组件类型与模板检查通过。

## 2. 接入认证页面

- [x] 2.1 在注册页传入当前用户名/邮箱执行密码校验，并在密码字段失焦和提交时显示错误、阻止不合规注册；通过代码检索和前端测试验证注册请求不会在校验失败时发出。
- [x] 2.2 在修改密码页传入当前账号上下文执行新密码校验，并在新密码字段失焦和提交时显示错误、阻止不合规改密；通过代码检索和前端测试验证改密请求不会在校验失败时发出。

## 3. 验证与收尾

- [x] 3.1 运行 `noj-ui` 的格式检查、lint、类型检查、完整测试和构建，确认密码提示改动不破坏现有认证页面。
- [x] 3.2 运行 `openspec validate surface-password-validation-errors --strict`、`git diff --check`，并确认注册/修改密码仍包含前端密码校验入口。
