## 1. 复现与测试基线（#292 / #293 / #294）

- [ ] 1.1 为“客观题套卷创建后添加小题并持久化”编写 noj-core 服务/路由测试，先跑一次确认当前是否已修复
- [ ] 1.2 为社区讨论/题解/动态发帖链路编写复现测试（启用社区、准备板块、授予权限），确认当前是否可发帖
- [ ] 1.3 为个人主页活跃度统计接口与前端展示编写测试/复现脚本，确认是空数据、接口错误还是渲染条件问题
- [ ] 1.4 记录复现结果，作为后续任务是否修改的依据

## 2. #279 编辑器草稿清除

- [ ] 2.1 修改 `useDraftStorage.clear()`：删除 localStorage 的同时清空 `code`、重置 `savedAt` 与 `state`
- [ ] 2.2 在 `EditorWorkspace.vue` 的 `@clear-draft` 处理中增加 `dialog.confirm` 确认弹窗
- [ ] 2.3 确认清除后重新拉取题目默认模板，并 `toast.success('草稿已清除')`
- [ ] 2.4 补充草稿清除相关测试（确认弹窗、清除后模板恢复、取消不清除）

## 3. #280 客观题提交记录

- [ ] 3.1 在 `ObjectiveAnswerForm.vue` 增加“提交记录”折叠区，复用现有 `/api/v1/problems/submissions?paper_id=...`
- [ ] 3.2 每条记录展示提交时间、得分，并可展开查看逐题答案与正误
- [ ] 3.3 竞赛模式提交记录遵循后端防泄题规则，不展示解析/标准答案
- [ ] 3.4 提交答案成功后自动刷新提交记录列表
- [ ] 3.5 补充前端/服务端相关测试

## 4. #284 私信未读与侧栏刷新

- [ ] 4.1 修改 `services/messages.ts`：`listConversations`、`getUnreadCount`、`getUnreadCountByConversation` 的未读 SQL 排除自己发送的消息
- [ ] 4.2 为未读计数补后端测试：自己发送不产生未读、对方发送产生未读、全部已读归零
- [ ] 4.3 `ChatSidebar.vue` 暴露 `refresh()` 方法供父页面调用
- [ ] 4.4 消息页发送成功后调用侧栏刷新
- [ ] 4.5 打开会话 `markAsRead` 成功后刷新侧栏未读
- [ ] 4.6 当前活跃会话收到 SSE `message:new` 时，加载消息后自动 `markAsRead` 并刷新红点

## 5. #285 私信关闭体验

- [ ] 5.1 修改 `routes/conversations.ts`：`/events` 在私信关闭时发送 `feature:disabled` 事件后关闭连接，不再直接 403
- [ ] 5.2 前端 `ChatSidebar` 与消息页处理 `feature:disabled`：停止 SSE/fallback 轮询，只提示一次
- [ ] 5.3 补充后端/前端测试：私信关闭时 SSE 收到关闭事件、普通 API 仍返回 403、前端不重复弹窗

## 6. #286 导航栏响应式

- [ ] 6.1 在 `Navbar.vue` 增加 `ResizeObserver` 计算可容纳导航项
- [ ] 6.2 放不下的导航项收进“更多”下拉菜单（使用 Nuxt UI dropdown/popover）
- [ ] 6.3 给导航项增加 `whitespace-nowrap`，避免文字换行/变列
- [ ] 6.4 保持 `md` 以下走现有侧边抽屉
- [ ] 6.5 手动验证窗口拉伸时导航项自适应、无跳动

## 7. #291 题单功能补全

- [ ] 7.1 扩展 `TrainingFormModal.vue` 支持编辑模式（标题/简介/可见性），复用 `updateTraining`
- [ ] 7.2 在“我的题单”页增加“编辑”入口
- [ ] 7.3 在题单详情页为创建者增加“编辑/改名”入口
- [ ] 7.4 复现/排查后台题单管理页“卡死”问题；若复现则修复
- [ ] 7.5 为“加入题单”交互和后端题单编辑补回归测试

## 8. #308 返回按钮样式统一

- [ ] 8.1 将 `community/posts/[postId].vue`、`community/bookmarks.vue` 的 `←/&larr;` 替换为 `UIcon i-lucide-arrow-left` + 文字
- [ ] 8.2 将 `problems/[id]/edit.vue`、`problems/new/objective.vue`、`problems/new/coding.vue` 等同类返回按钮统一
- [ ] 8.3 全局搜索 `&larr;`/`←`，确认无遗漏

## 9. #293 社区发帖可靠性

- [ ] 9.1 根据 1.2 复现结果修复发帖链路中发现的 bug（如有）
- [ ] 9.2 前端在板块为空、权限不足、服务端校验失败时显示明确错误，不静默失败
- [ ] 9.3 若确认缺省讨论板块，补充默认板块种子或迁移
- [ ] 9.4 补充社区发帖失败/成功回归测试

## 10. #294 个人主页活跃度

- [ ] 10.1 活跃度卡片改为无条件渲染；接口失败/无数据时显示“暂无签到数据”占位
- [ ] 10.2 增加重试入口与错误态展示
- [ ] 10.3 按 issue 优化活跃度卡片 UI
- [ ] 10.4 补充活跃度卡片渲染测试

## 11. 全量验证与收尾

- [ ] 11.1 运行 `deno fmt` 与 `deno lint`（noj-core / noj-ui）
- [ ] 11.2 运行 noj-core 全量测试（`deno task test`）
- [ ] 11.3 运行 noj-ui 构建（`nuxt build` 或等价任务）
- [ ] 11.4 手动回归：草稿清除、客观题提交记录、私信未读/关闭、导航栏、题单编辑、社区发帖、活跃度、返回按钮
- [ ] 11.5 按 issue 拆分提交信息，准备 PR